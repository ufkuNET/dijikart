# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=qrtoo
DB_USER=root
DB_PASSWORD=''

# Session Configuration
SESSION_SECRET=your_super_secret_session_key_here_make_it_long_and_random_at_least_32_characters

# Admin Configuration
ADMIN_PASSWORD=your_secure_admin_password_here

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
ALLOWED_ORIGINS=https://qrtoo.de,https://www.qrtoo.de

# Mail Configuration (if using mail features)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=atiktahir2020@gmail.com
MAIL_PASS=hrti itoi jzzf pdnh

# Security Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
LOGIN_RATE_LIMIT_MAX=3
LOGIN_RATE_LIMIT_WINDOW_MS=300000

# File Upload Configuration
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/gif,image/webp

# Logging Configuration
LOG_LEVEL=error
LOG_RETENTION_DAYS=7

# Production Configuration
PRODUCTION_URL=https://qrtoo.de
SECURE_COOKIES=true

# Security Headers
STRICT_TRANSPORT_SECURITY=true
CONTENT_SECURITY_POLICY=true
X_FRAME_OPTIONS=DENY
X_CONTENT_TYPE_OPTIONS=nosniff
X_XSS_PROTECTION=1; mode=block

# Rate Limiting
API_RATE_LIMIT_MAX=100
API_RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX=3
LOGIN_RATE_LIMIT_WINDOW_MS=300000

# Database Pool Configuration
DB_POOL_MAX=10
DB_POOL_MIN=0
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# SSL Configuration
SSL_ENABLED=true
SSL_KEY_PATH=/path/to/private.key
SSL_CERT_PATH=/path/to/certificate.crt

# Security Warnings
# ⚠️  NEVER use hardcoded passwords in code!
# ⚠️  NEVER commit .env files to version control!
# ⚠️  ALWAYS use environment variables for sensitive data!
# ⚠️  CHANGE default passwords immediately!

